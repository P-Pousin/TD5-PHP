<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8"/>
        <title>Social Sondage</title>
        <link rel="stylesheet" href="/dist/css/bootstrap.min.css" />
        <link rel="stylesheet" href="/style/style.css" />
        <link rel="icon" type="image/x-icon" href="/favicon.ico" />
	</head>
	<body>
		<h1>
        	<img src="/imgs/poll.png" />
        	Social <span class="sondage">Sondage</span>
    	</h1>
    	<nav class="navbar navbar-default">

	        <!-- Collect the nav links, forms, and other content for toggling -->
	        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
	          <ul id="menu" class="nav navbar-nav">
	            <li
	            {% if app.request.requestUri == path('index') %}
	            	class="active" 
	            {% endif %}
	            ><a href="{{ path('index') }}">Accueil</a></li>
	            <!-- Si l'utilisateur est connecté -->
	           	{% if app.session.get('user') %}
	            	<li
	            	{% if app.request.requestUri == path('logout') %}
	            		 class="active" 
	            	{% endif %}
	            	><a href="{{ path('logout') }}">Déconnexion</a></li>
	            	<li
	            	{% if app.request.requestUri == path('create-form') %}
	            		class="active" 
	            	{% endif %}
	            	><a href="{{ path('create-form') }}">Nouveau sondage</a></li>
	            {% else %}
	            	<li
	            	{% if app.request.requestUri == path('register-form') %}
	            		class="active" 
	            	{% endif %}
	            	><a href="{{ path('register-form') }}">Inscription</a></li>
	            	<li
	            	{% if app.request.requestUri == path('login-form') %}
	            		class="active" 
	            	{% endif %}
	            	><a href="{{ path('login-form') }}">Identification</a></li>
	            {% endif %}
	            <li
	            {% if app.request.requestUri == path('polls') %}
	            	class="active" 
	            {% endif %}
	            ><a href="{{ path('polls') }}">Sondages</a></li>
	          </ul>
	        </div>
    	</nav> 
    	
		{% block content %}{% endblock %}

		<footer class="col-lg-12">
			
		</footer>	
	</body>
</html>	